# Memory — 2026-03-12

## Dashboard Fix
- Root cause: Star Office added `127.0.0.1:19001` reverse proxy block to Caddyfile, triggering Caddy auto-HTTPS → tried to bind port 80 → permission denied → crash loop
- Fix: added `{ auto_https off }` global block to `/home/midang/.config/caddy/Caddyfile`
- API (`:45681/health`) was unaffected; only Caddy (`:45680`) was down

## OpenClaw Upgrade
- We're on v2026.3.2, latest is v2026.3.8 (6 patches behind)
- Key fixes in v2026.3.8: Telegram DM dedup, cron/Telegram false-delivered fix, browser relay WSL2 `relayBindHost`, secrets config persistence after writes
- Key features: `openclaw backup create/verify`, Brave `llm-context` mode, ACP provenance
- Upgrade scheduled via one-shot cron at 18:40 UTC 2026-03-11 (cron ID `03665e4e-7ed5-482a-8d0a-6f917d0bfc38`, auto-delete after run)
- Script: `workspace-spider/scripts/openclaw-upgrade.sh`

## Team Roles & Roster
- `team-roles.md` built at `~/.openclaw/skills/team-workflow/references/team-roles.md` — single source of truth for all 10 agents
- All 10 USER.md files updated with current roster + link to team-roles.md
- King SOUL.md updated: product vision/mission, daily top 3, architecture gatekeeper added
- Announced to #product-team

## Alex Daily Monitoring
- Alex dispatched to add OpenClaw releases to daily search routine
- Should check `https://github.com/openclaw/openclaw/releases`, summarize features/fixes/security
- Post to #product-team + sessions_send to agent:spider:main on new releases

## Star Office / Caddyfile
- Star Office backend: port 19000 (python3), reverse proxy via Caddy on 19001
- Star Office join keys (short format): ocj_oz, ocj_john, ocj_king, ocj_joe, etc. (all agents synced)
- `/home/midang/.openclaw/scripts/star-office-push.py` — push state script

## Ongoing Tasks (as of 2026-03-12)
- OpenClaw upgrade: scheduled 18:40 UTC 2026-03-11 (likely completed, verify)
- SRA-001 T2 (Enneagram + synthesis skills): still pending
- SRA-001 T3 (Dev: calculation scripts): still pending
- SRA-001 T5 (John→Seer Telegram routing): still pending
- Ford reference library: dirs created, content empty
- Seer compaction threshold fix: global softThresholdTokens:8000 too low for Seer (~37K workspace)

## Incident: Stale Upgrade Cron (2026-03-12)
- Root cause: LLM cron ran `openclaw gateway stop` → killed its own session → never self-deleted → leaked warning messages for 19h
- Fix: deleted cron job `03665e4e` manually
- Prevention rules added to AGENTS.md:
  1. Never use LLM cron for gateway restarts — use exec in-session
  2. Verify one-shot cron cleanup within 10min of expected run
  3. No cron job may stop/restart the gateway
  4. Tag disruptive crons: `DISRUPTIVE-<name>` convention
